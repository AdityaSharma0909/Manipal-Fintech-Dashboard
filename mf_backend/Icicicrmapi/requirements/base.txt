# =============================================================================
# Base Requirements — shared across all environments
# =============================================================================

# Django core
Django==5.0.6
djangorestframework==3.15.2
django-cors-headers==4.4.0
django-environ==0.11.2
django-filter==24.2
drf-spectacular==0.27.2

# Database
psycopg2-binary==2.9.9               # PostgreSQL adapter

# JWT Authentication
PyJWT==2.8.0
djangorestframework-simplejwt==5.3.1

# HTTP clients
requests==2.32.3
httpx==0.27.0

# Cryptography / Encryption
cryptography==42.0.8

# Serialization / Validation
marshmallow==3.21.3                   # DTO validation (optional complement to DRF serializers)

# Utilities
python-decouple==3.8                  # Alternative env var loader
python-dateutil==2.9.0

# Retry / resilience
tenacity==8.3.0                       # Retry decorator with backoff

# Logging
structlog==24.1.0                     # Structured JSON logging

# Unique IDs / correlation
uuid==1.30

# Timezone
pytz==2024.1
